import { FiltrarPessoasPipe } from './filtrar-pessoas.pipe';

describe('FiltrarPessoasPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltrarPessoasPipe();
    expect(pipe).toBeTruthy();
  });
});
